// components/ui/AuroraBackground.tsx
// Removed — monochrome theme uses dot-grid instead
// Keeping empty export to avoid breaking imports during transition

export function AuroraBackground() {
  return null;
}
